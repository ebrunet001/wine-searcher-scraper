{
    "title": "Wine Searcher Domain Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "domain_url": {
            "title": "Domain/Merchant URL",
            "type": "string",
            "description": "URL of the wine domain/merchant page (e.g., https://www.wine-searcher.com/merchant/22643-domaine-de-la-romanee-conti)",
            "editor": "textfield",
            "prefill": "https://www.wine-searcher.com/merchant/22643-domaine-de-la-romanee-conti"
        },
        "max_wines": {
            "title": "Maximum wines to scrape",
            "type": "integer",
            "description": "Maximum number of wines to scrape (0 for all)",
            "default": 0,
            "minimum": 0
        },
        "include_analytics": {
            "title": "Include Analytics (Search Rank)",
            "type": "boolean",
            "description": "Fetch search rank from analytics tab (slower but more data)",
            "default": true
        },
        "use_proxy": {
            "title": "Use Residential Proxy",
            "type": "boolean",
            "description": "Use Apify residential proxy to bypass anti-bot protection (recommended)",
            "default": true
        }
    },
    "required": ["domain_url"]
}
